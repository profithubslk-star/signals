# ProfitHub Signals

This repository is automatically updated by the ProfitHub Signal Bot.

## Files
- `signals.json` – Live signal data (used by website)
- `bot.py` – Signal generator bot (runs on VPS)
- `images/` – Signal images
- `requirements.txt` – Python dependencies

⚠️ `.env` is intentionally excluded for security.
